{
  "scripts": {
    "dev": "concurrently --kill-others \"cd app && npm run start:watch\" \"cd widgets && npm run bundle:watch\" \"cd app && npm run build:watch\" \"y-websocket\""
  },
  "dependencies": {
    "rollup-plugin-import-css": "^3.1.0"
  }
}
